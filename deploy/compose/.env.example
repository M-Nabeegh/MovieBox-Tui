MOVIEBOX_DATA_ROOT=/srv/moviebox
MOVIEBOX_BIND=0.0.0.0:8420
MOVIEBOX_MAX_HEIGHT=1080
MOVIEBOX_DOWNLOAD_CONCURRENCY=1
MOVIEBOX_RESERVE_GIB=10
# Subtitle language attached when a download does not name one.
# Defaults to English when unset; use `off` to disable automatic subtitles.
MOVIEBOX_SUBTITLE_LANGUAGE=english
# Optional features. Create the secret file with `umask 077`, then uncomment
# the matching line. Files must be mode 0600 and non-empty.
# Jellyfin API key: lets the server trigger a library scan after each download.
#MOVIEBOX_JELLYFIN_API_KEY_FILE=/optional-secrets/jellyfin_api_key.txt
# MCP bearer token: enables the agent endpoint at /mcp. Unset keeps it disabled.
#MOVIEBOX_MCP_TOKEN_FILE=/optional-secrets/mcp_token.txt
# Completion notifications: a webhook posted when a download is ready to watch.
# The URL carries a token, so it lives in a secret file rather than here.
#MOVIEBOX_NOTIFY_WEBHOOK_URL_FILE=/optional-secrets/notify_webhook_url.txt
# Name used in the message, e.g. "Hey Nabeegh, Cocktail 2 (2026) is ready to watch."
#MOVIEBOX_NOTIFY_RECIPIENT=Nabeegh
# Where tapping the notification goes.
#MOVIEBOX_NOTIFY_LINK_URL=https://your-host.ts.net:8443/
MOVIEBOX_UID=1000
MOVIEBOX_GID=1000
JELLYFIN_RENDER_GID=993
JELLYFIN_PUBLISHED_PORT=8096
